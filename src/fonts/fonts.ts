import { Anton, Caveat_Brush, Limelight, Amarante } from "next/font/google";

export const anton = Anton({
  subsets: ["latin"],
  weight: ["400"],
});

export const caveatBrush = Caveat_Brush({
  subsets: ["latin"],
  weight: ["400"],
});

export const limelight = Limelight({
  subsets: ["latin"],
  weight: ["400"],
});

export const amarante = Amarante({
  subsets: ["latin"],
  weight: ["400"],
});
